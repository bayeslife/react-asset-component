{
  "name": "short-interval-control-react-assets-component",
  "version": "1.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "nwb serve-react-demo",
    "lint": "eslint --ext .jsx --ext .js --fix src test",
    "transpile": "babel src -d dist --copy-files",
    "prepublishOnly": "npm run lint && npm run transpile",
    "pack": "webpack --mode production",
    "github": "gh-pages -d examples/dist",
    "build": "npm run lint && npm run transpile && npm run pack",
    "deploy": "npm run build && npm run github",
    "test": "riteway -r @babel/register -r @babel/polyfill test/**/*.test.js*",
    "watch": "nodemon --exec 'npm test'"
  },
  "peerDependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "author": "bayeslife",
  "license": "MIT",
  "devDependencies": {
    "short-interval-control-archetype-react-component-dev": "^1.0.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "short-interval-control-archetype-react-component": "^1.0.0"
  }
}
